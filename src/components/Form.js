// Import dependencies
import React from 'react';
import styled from 'styled-components';

// Build Form
function Form(props) {
	return (
		<>
			<p>form</p>
		</>
	);
};

// Export Form
export default Form;