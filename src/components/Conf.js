// Import dependencies
import React from 'react';
import styled from 'styled-components';

// Build Confirmation
function Conf(props) {
	return (
		<>
			<p>confirmation screen</p>
		</>
	);
};

// Export Confirmation
export default Conf;